import Card from "../components/card";

export default function HomeScreen() {
  /**
   * Fetch data from tables with useEffect
   * Store the data in an array
   * Conditional render of the tables
   */
  return (
    <>
      <Card title="Hola" img="" table_id={1} />
    </>
  );
}
